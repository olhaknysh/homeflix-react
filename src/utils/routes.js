const routes = {
  home: '/',
  register: '/account/register',
  login: '/account/login',
  account: '/account',
  favorites: '/favorites',
  search: '/search',
  showDetails: '/show/:showId',
  updates: '/updates',
  all: '/all',
};

export default routes;
